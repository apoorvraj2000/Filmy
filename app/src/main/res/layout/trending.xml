<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="12dp"
    android:background="@color/black">
    <ImageView
        android:id="@+id/imageTrend"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
       android:src="@drawable/poster"
    app:layout_constraintTop_toTopOf="parent"
    app:layout_constraintLeft_toLeftOf="parent"
    app:layout_constraintRight_toRightOf="parent"/>

    <TextView
        android:id="@+id/ratingTrend"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginEnd="20dp"
        android:text="6.8"
        android:textSize="26sp"
        android:padding="10dp"
        app:layout_constraintRight_toRightOf="@id/imageTrend"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_gravity="center_horizontal|bottom"
        android:background="#5B000000"
        android:textColor="#ffffff00"/>
    <TextView
        android:id="@+id/titleTrend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"

        android:background="#5B000000"
        android:padding="6dp"
        android:text="Title"
        android:textColor="#ffff00"
        android:textSize="30sp"
        app:layout_constraintBottom_toTopOf="@id/releaseTrend"
        app:layout_constraintLeft_toLeftOf="@id/imageTrend"
        app:layout_constraintRight_toRightOf="@id/imageTrend" />

    <TextView
        android:id="@+id/releaseTrend"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|bottom"
        android:layout_marginBottom="2dp"
        android:background="#5B000000"
        android:paddingStart="4dp"
        android:padding="4dp"
        android:text="Date"
        android:textColor="#ffffff00"
        android:textSize="18sp"
        app:layout_constraintBottom_toBottomOf="@id/imageTrend" />

</androidx.constraintlayout.widget.ConstraintLayout>